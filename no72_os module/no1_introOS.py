import os

os.mkdir("dataF") # if folder dataF is already present so it show some error( error for saying that the given named folder is already present in currently working directory soo you can't create another same named folder again)

